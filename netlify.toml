# Netlify build config — static export of the Next.js portfolio.
[build]
  command = "npm run build"
  publish = "out"

[build.environment]
  NODE_VERSION = "20"

# SPA-style fallback is unnecessary for a single static page, but a 404
# is handled by Next's exported not-found page.
